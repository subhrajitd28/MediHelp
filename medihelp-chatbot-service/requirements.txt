# ── Web Framework ─────────────────────────────────────────────────────────────
flask==3.1.1
flask-cors==4.0.0

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain==0.3.26
langchain-core==0.3.80
langchain-community==0.3.26
langchain-text-splitters==0.3.11
langchain-pinecone==0.2.8
langchain-groq==0.2.0
langsmith==0.4.49

# ── Vector DB ─────────────────────────────────────────────────────────────────
pinecone[asyncio]==7.3.0

# ── LLM / Embeddings ──────────────────────────────────────────────────────────
sentence-transformers==2.3.1
torch==2.8.0
transformers==4.36.2

# ── PDF & OCR ─────────────────────────────────────────────────────────────────
pypdf==5.6.1
pytesseract==0.3.13
pillow==12.0.0

# ── HTTP Client (voice + image API calls) ─────────────────────────────────────
httpx==0.27.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.1.0

# ── Service Discovery (registers with medihelp-gateway's Eureka) ──────────────
py-eureka-client==0.11.10

# ── NOTE: SQLite is built into Python — no install needed ─────────────────────
# ── NOTE: Tesseract binary must be installed separately ───────────────────────
#    Mac:   brew install tesseract
#    Linux: sudo apt install tesseract-ocr
